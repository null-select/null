# Security

Please do not report suspected vulnerabilities in a public issue. Email `security@null.select` with a minimal reproduction and impact assessment.

The CLI stores its execution-bound workload credential in the operating system user configuration directory with owner-only permissions. Enrollment and workload tokens must never be included in issue reports, logs, screenshots, or shell history.
